{
  "name": "pixaki",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "rimraf dist && tsc && rimraf dist/interfaces && node chmod && cp src/package.json dist/package.json && cp src/README.md dist/README.md",
    "test": "npm run build && sudo npm link && pixaki export test/pixaki-projects/crate.pixaki"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "pixaki": "./dist/cli.js"
  },
  "dependencies": {
    "@types/yargs": "^16.0.1",
    "imagemagick": "^0.1.3",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@types/imagemagick": "0.0.30",
    "@types/node": "^14.14.41",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
